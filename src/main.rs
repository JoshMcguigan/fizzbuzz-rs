// macro (generate struct or method for each number??)
// plain normal way
// enum (create custom enum type with fizz, buzz, and fizzbuzz variants, then write from/into, etc)
// also pull out basic number -> String code into separate function to discuss on its own
// lifetimes version where number is passed in as reference and &str is passed back
// build.rs version

// what is this?
// "a tour of rust via fizz buzz"
// "six(?) ways to fizz buzz with rust"
// "learn rust via fizz buzz"

mod func;
mod flexible;
mod display;
mod intro;
mod enumeration;

fn main() {

}
